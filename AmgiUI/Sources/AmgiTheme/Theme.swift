public enum Theme: String, CaseIterable, Sendable {
    case vivid
    case muted
    case legacy
}

public enum Appearance: String, CaseIterable, Sendable {
    case system
    case light
    case dark
}
